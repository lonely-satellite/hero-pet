// @flow

/*::
export type Bonus = { name: string } & (
  | { type: 'health', healthBonus: number }
  | { type: 'armor', armorBonus: number }
  | { type: 'damage', damageBonus: number }
  | { type: 'speed', speedBonus: number }
  | { type: 'healthRegen', healthRegenBonus: number }
);
*/